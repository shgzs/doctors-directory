# PROJECT_CONTEXT.md — راهنمای ادامه‌ی پروژه با یک دستیار AI دیگر

> این فایل رو کامل به دستیار جدید (مثلاً ChatGPT) بده و بگو: «این سند وضعیت یک پروژه‌ی نرم‌افزاری‌ست که با یک AI دیگه شروع کردم. لطفاً همه‌ی این‌ها رو بخون، معماری و تصمیم‌ها رو به‌عنوان مبنا بپذیر، و از همین‌جا با همین قراردادها ادامه بده.»

## ۱. معرفی پروژه

یک وب‌سایت برای گروهی از دوستان/همکلاسی‌های پزشک که سال‌ها بعد از فارغ‌التحصیلی دوباره دور هم جمع شده‌اند. هدف:

1. **دایرکتوری اعضا**: نام، تخصص، شهر، شماره تلفن عمومی (برای بیماران) و شخصی (فقط برای اعضای وارد‌شده)، شماره نظام پزشکی، ایمیل، محل‌های کار (با روزهای هفته)، شبکه‌های اجتماعی، و فیلدهای آزاد/گسترش‌پذیر برای هرچیز دیگه (زیرتخصص، اطلاعات خانوادگی و غیره).
2. **کارت ویزیت خودکار**: هر عضو می‌تونه پروفایلش رو کامل کنه و یک کارت ویزیت (احتمالاً SVG، قابل تبدیل به PNG/PDF) با انتخاب قالب داشته باشه.
3. **ورود با OTP**: بدون رمز عبور. شماره موبایل وارد می‌شه، کد پیامک می‌شه، تایید می‌شه.
4. **سطح دسترسی**: کاربر مهمان فقط اطلاعات عمومی می‌بینه؛ عضو تایید‌شده اطلاعات شخصی (شماره خصوصی، فیلدهای با visibility="members") رو هم می‌بینه.
5. **معرفی‌نامه‌ها (Referrals)**: بخش جدا برای معرفی پزشکان (لزوماً عضو گروه نیستند) به یکدیگر، قابل جست‌وجو بر اساس تخصص/شهر، فقط برای اعضای وارد‌شده قابل مشاهده.
6. **آدرس‌های زیبا و قابل‌اشتراک**: هر پروفایل یک URL کوتاه و قابل تبدیل به QR کد داره تا بشه به بیمار داد.

## ۲. تصمیم‌های فنی گرفته‌شده (این‌ها مبنا هستند، تغییرشون نیاز به دلیل جدید داره)

| موضوع | تصمیم | دلیل |
|---|---|---|
| میزبانی/زبان | Cloudflare Workers + Hono (TypeScript) | کاربر از قبل اکانت Cloudflare داشت؛ Google Sites رد شد چون هیچ دیتابیس/بک‌اندی نداره |
| دیتابیس | Cloudflare D1 (SQLite) | رایگان، بدون نیاز به کارت اعتباری |
| ذخیره‌ی موقت OTP | Cloudflare KV (`OTP_KV`) | TTL کوتاه، مناسب همین کار |
| ذخیره‌ی تصاویر (آواتار) | Cloudflare KV (`ASSETS_KV`) | این پروژه از R2 استفاده نمی‌کند و آواتارها در KV نگه‌داری می‌شوند. |
| احراز هویت | JWT دست‌ساز با Web Crypto (بدون کتابخانه‌ی خارجی) — چون روی Workers runtime بهتر جواب می‌ده | |
| سرویس پیامک OTP | ملی‌پیامک (Melipayamak)، متد REST به آدرس `https://rest.payamak-panel.com/api/SendSMS/SendOtp` با POST و بدنه‌ی form-urlencoded. پاسخ JSON با فیلد `RetStatus` (۱ یعنی موفق) | کاربر اکانت ملی‌پیامک داره؛ Twilio برای شماره ایرانی مناسب نبود |
| عضویت اعضای جدید | شماره‌های از پیش لیست‌شده (`preapproved_phones`) خودکار approve می‌شن؛ شماره‌های جدید نیاز به تایید دستی ادمین دارن (`status='pending'`) | تصمیم صریح کاربر |
| دسترسی به بخش معرفی‌نامه‌ها | فقط اعضای وارد‌شده (نه عمومی) | تصمیم صریح کاربر |
| مدل داده‌ی فیلدهای اضافه | ترکیب دو لایه: ستون‌های ثابت برای فیلدهای پرکاربرد + جدول `dynamic_fields` (key/value + سطح دسترسی per-field) برای هرچیز دیگه که بعداً لازم بشه، بدون نیاز به migration | کاربر گفت می‌خواد بعداً بتونه فیلد اضافه کنه بدون تغییر ساختار |
| آدرس‌دهی پروفایل | دو لایه: (۱) لینک canonical همیشه‌کارکن `/d/{id}` یا `/doctors/{id}` روی هر دامنه، مناسب QR کد. (۲) لینک زیبا: `radio.logist.ir/ghasemi` (وقتی ساب‌دامین شناخته‌شده باشه) یا `هردامنه/radio/ghasemi` (fallback مسیر-محور روی دامنه‌های دیگه). منطق تشخیص در `src/routes/resolve.ts`. | دامنه‌ی `logist.ir` مال کاربره؛ ولی سایت قراره روی دامنه‌های دیگه هم قابل دیدن باشه، پس همه‌چیز باید domain-agnostic باشه و هیچ‌وقت ۴۰۴ خام نده |
| slug یکتایی | `UNIQUE(specialty_id, slug)` — یعنی نام مستعار فقط باید در همون تخصص یکتا باشه، نه کل سایت | |
| حالت دیباگ | متغیر عمومی `DEBUG_MODE` در `wrangler.toml` — وقتی `"true"` باشه، ارسال SMS متوقف می‌شود، کد OTP و جزئیات خطا برای توسعه نمایش داده می‌شوند | فقط برای توسعه |

## ۳. کارهایی که هنوز انجام نشده (آگاهانه به بعد موکول شده)

- اتصال دامنه‌ی واقعی `logist.ir` و ساخت رکورد Wildcard DNS
- راه‌اندازی GitHub repository و CI/CD
- فرانت‌اند (فرم ورود، تکمیل پروفایل، صفحه‌ی لیست/جست‌وجوی پزشکان، صفحه‌ی معرفی‌ها، پنل ادمین)
- تولید واقعی کارت ویزیت SVG (فقط طراحی مفهومی‌ش انجام شده: باید از روی اطلاعات پروفایل، بدون نیاز به ذخیره‌سازی فایل، هر بار تولید بشه)
- تولید QR کد از URL پروفایل

## ۴. ساختار فعلی کد (بک‌اند)

```
doctors-directory/
  wrangler.toml          # تنظیمات D1 / KV / متغیرها — شرح کامل هر binding داخل کامنت‌هاش هست
  schema.sql              # اسکیمای کامل دیتابیس (جداول: doctors, specialties, work_locations,
                           #   social_links, dynamic_fields, referrals, preapproved_phones)
  README.md                # راهنمای فارسیِ کامل راه‌اندازی، قدم‌به‌قدم
  src/
    index.ts               # اپ اصلی Hono، اتصال همه‌ی route ها، هندلر خطای سراسری
    types.ts                # تعریف Env bindings
    lib/
      jwt.ts                # امضا/تایید JWT با Web Crypto (بدون وابستگی خارجی)
      phone.ts               # نرمال‌سازی شماره ایرانی (0912... <-> 98912...) + تولید کد OTP
      sms.ts                  # ارسال OTP از طریق ملی‌پیامک، با پارس RetStatus واقعی
      middleware.ts            # attachAuth / requireApprovedMember / requireAdmin
      subdomain.ts              # استخراج لیبل ساب‌دامین از hostname
    routes/
      auth.ts                   # POST /api/auth/request-otp ، /verify-otp
      doctors.ts                  # GET لیست/جزئیات (با فیلتر خودکار عمومی/عضو) + PUT پروفایل خودم
                                   #   + profile, work-locations, social-links, extra-fields
      assets.ts                    # آپلود آواتار (KV) + سرو تصویر
      referrals.ts                  # لیست/ثبت معرفی‌نامه (فقط اعضا)
      admin.ts                       # لیست/تایید/رد اعضای در انتظار
      resolve.ts                     # منطق آدرس‌دهی زیبا (توضیح کامل در جدول بالا)
```

## ۵. قراردادهای مهم که باید رعایت بشه

- همه‌ی متن‌ها/کامنت‌های راهنما برای کاربر باید **فارسی** باشن (کاربر فارسی‌زبانه).
- پاسخ خطاها همیشه پیام فارسیِ قابل‌فهم برمی‌گردونن، نه فقط کد HTTP.
- هیچ endpoint‌ای نباید خطای خام سرور بده؛ همیشه JSON با پیام مشخص (فعلاً یک هندلر global در `index.ts` این کار رو می‌کنه، هرچند فعلاً stack trace رو هم برای دیباگ echo می‌کنه — باید قبل از production trim بشه).
- شماره تلفن همیشه به فرمت نرمال‌شده‌ی `98XXXXXXXXXX` ذخیره و مقایسه می‌شه (تابع `normalizePhone`).
- فیلدهای حساس/شخصی (شماره خصوصی، فیلدهای dynamic با visibility غیر از public) هیچ‌وقت در پاسخ‌های عمومی (کاربر واردنشده) نباید لو برن — این منطق در `doctors.ts` (فیلتر `isApprovedMember`) پیاده شده و باید در هر endpoint جدید هم رعایت بشه.
- توسعه‌ی آینده باید همیشه **بدون نیاز به کارت اعتباری** پیش بره مگر این‌که کاربر صراحتاً بگه هزینه کنه.

## ۶. وضعیت فعلی تست (تا این لحظه)

- بک‌اند لوکال با `npx wrangler dev --remote` تست شده (به D1/KV واقعی وصل، نه شبیه‌سازی لوکال — این نکته مهمه چون حالت لوکالِ `wrangler dev` بدون `--remote` دیتابیس خالی جدا داره).
- جریان OTP کامل تست شده (در حالت `DEBUG_MODE=true` برای توسعه).
- توسعه‌ی محلی با `npx wrangler dev` از D1/KV محلی استفاده می‌کند؛ migrationها در پوشه‌ی `migrations/` هستند و seed دو کاربر محلی در `seed.local.sql` قرار دارد.
- ورود، تایید، تبدیل به ادمین دستی، همه با موفقیت تست شدن.
- یک مشکل انکودینگ UTF-8 (فارسی به‌صورت `؟؟؟` ذخیره می‌شد) کشف شد — علتش خودِ D1/SQLite نبود، بلکه احتمالاً انکودینگ ارسال درخواست از PowerShell کاربر بود؛ راه‌حل پیشنهادی: تبدیل صریح بدنه‌ی JSON به بایت UTF-8 قبل از ارسال (`[System.Text.Encoding]::UTF8.GetBytes(...)`) — هنوز تایید نهایی نشده که این کاملاً مشکل رو حل کرده.

## ۷. لحن و سبک کار با کاربر

- کاربر مهندس/فنی نیست، ولی SQL بلده (فقط با MSSQL/SSMS آشنا نه SQLite/D1) و می‌تونه دستورهای ترمینال رو دنبال کنه.
- ترجیح می‌ده به‌جای ارسال کل پروژه هر بار، فقط فایل‌های تغییرکرده/جدید رو بگیره (برای صرفه‌جویی در توکن دستیار AI).
- روی ویندوز کار می‌کنه (PowerShell)، نه Mac/Linux — دستورهای curl باید فرمت ویندوزی/PowerShell داشته باشن.
- ترجیح می‌ده هر تصمیم فنی مهم (مثل انتخاب سرویس پیامک، سطح دسترسی بخش‌ها) رو خودش صراحتاً تایید کنه، نه این‌که AI خودسرانه تصمیم بگیره.
